session: whill

root: ~/ros_workspaces/whill_ros

# before_start:
  # - echo "test"
  # - . ~/ros/devel/setup.bash

stop:
  - echo "finish"

windows:
  - name: code
    layout: main-horizontal
    commands:
      - . ~/ros_workspaces/whill_ros/devel/setup.bash
    panes:
      - type: vertical
        commands:
          - echo "pane 1"
          - cd ~/ros_bags/
      - type: vertical
        commands:
          - echo "pane 2"
      - type: vertical
        commands:
          - echo "pane 3"

