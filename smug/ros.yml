session: ros

root: ~/ros/src

before_start:
  - echo "test"
  # - . ~/ros/devel/setup.bash

stop:
  - echo "finish"

windows:
  - name: code
    layout: tiled
    commands:
      - . ~/ros/devel/setup.bash
    panes:
      - type: tiled
        commands:
          - echo "pane 1"
          - cd ~/ros_bags/
      - type: tiled
        commands:
          - echo "pane 2"
      - type: tiled
        commands:
          - echo "pane 3"
